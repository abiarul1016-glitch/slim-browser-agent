[project]
name = "slim-browser-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "git-filter-repo>=2.47.0",
    "ollama>=0.6.1",
    "playwright>=1.58.0",
]
